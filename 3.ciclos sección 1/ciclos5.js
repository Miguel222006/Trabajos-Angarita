let numero = parseInt(prompt("Ingrese un número"))

if (numero % 2 === 0) {    // el numero par debe dar 0 en la division al dividirlo entre 2, si no da 0 sera impar
    alert(`Este numero es par`)
} 
      
else {
    alert(`Este numero es impar`)
}
